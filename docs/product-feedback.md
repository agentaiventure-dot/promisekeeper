# Feedback: Nebius Token Factory, NVIDIA Nemotron, Tavily

Written during the Nebius x NVIDIA Global AI Hackathon, September 2026. Updated as the build progresses.

## Token Factory
- (to fill after the first real run) time from key to first successful `chat/completions` call; whether `response_format: json_schema` with `strict: true` is honoured by the Nemotron endpoint or silently ignored; latency and token usage for a 2,000-character transcript on Nano vs Super vs Ultra.

## Nemotron
- (to fill) extraction accuracy on the five fixture transcripts: quotes verbatim, dates resolved correctly, offers not mislabelled as commitments; how often the quote-verification guard fires.

## Tavily
- (to fill) result quality for "<company> formal complaint process escalate ombudsman regulator".

## Requests
- (to fill)
